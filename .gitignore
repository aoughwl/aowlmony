node_modules/
*.log
vendor/
.nifmony/
.aifmony/
# emitted idiomatic-backend sources (aowlts/aowljs/aowlpy) beside examples
examples/*.ts
examples/*.js
examples/*.py
DESIGN.md
ROADMAP.md

# Nimony build output. The binary comes from ./build.sh and, once it reaches
# parity, ships as a release asset — it is never committed.
nimcache/
bin/aowlmony-ng
.aowlmony/

# Machine-local include paths for the editor tooling (build.sh takes AOWLKIT).
.aowlcode.flags

# Release artifacts. Built by ./release.sh and uploaded as release assets;
# a binary in git is a binary someone eventually ships by accident.
dist/

# …including the one beside examples/. Both trees were tracked by accident: 143
# files of compiler scratch, referenced by nothing, in a public repo — and one
# path carried a local scratchpad session id.
examples/nimcache/
